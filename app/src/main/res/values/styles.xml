<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavigationMapRoute">
        <!-- Colors -->
        <item name="routeColor">#1e88e5</item>
        <item name="routeModerateCongestionColor">#ffeb3b</item>
        <item name="routeSevereCongestionColor">@color/colorAccent</item>
        <item name="routeShieldColor">#FFFFFF</item>
        <!-- Scales -->
        <item name="routeScale">1.0</item>
    </style>

    <style name="NavigationView" parent="AppTheme.NoActionBar">
        <item name="navigationViewPrimary">#FFFFFF</item>
        <item name="navigationViewSecondary">#1e88e5</item>
        <item name="navigationViewAccent">@color/colorAccent</item>
    </style>

</resources>
